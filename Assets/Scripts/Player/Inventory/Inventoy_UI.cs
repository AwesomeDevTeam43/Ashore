using UnityEngine;

// Legacy inventory UI (removed). Kept as an empty stub to break compile-time dependencies safely.
[System.Obsolete("Legacy Inventoy_UI has been removed. Use MenuController + InventoryPage.")]
public class Inventoy_UI : MonoBehaviour
{
}